# Server Configuration
PORT=3000
NODE_ENV=development # development, production, test
BASE_URL=http://localhost:3000 # Base URL for constructing short links

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=linkshort
# DATABASE_URL=postgresql://your_db_user:your_db_password@localhost:5432/linkshort # Alternative

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
# REDIS_URL=redis://localhost:6379 # Alternative

# Security
JWT_SECRET=your_very_strong_jwt_secret_key # For user authentication (if implemented)
JWT_EXPIRES_IN=1d # e.g., 1d, 7d, 30m

# Rate Limiting (Optional - if you implement it)
# RATE_LIMIT_WINDOW_MS=15 * 60 * 1000 # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100 # Max requests per window per IP
